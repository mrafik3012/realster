# Incorporated agent skills

Used on this build (not just listed):

- **UI-UX Pro Max** — brand + design-system + ui-styling.
  Applied in `docs/brand-guidelines.md`, `docs/design-tokens.json`,
  `src/styles/design-tokens.css`, and the survey-grid / land-first layout.
- **Remotion** — create + markup + render.
  Composition lives in `remotion/`. Output: `public/videos/hero.mp4` + `.webm`.
